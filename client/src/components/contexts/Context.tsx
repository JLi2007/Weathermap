import { createContext} from "react";


export const mapContext = createContext('');

// export const addMarker=([lat,lon]) => {

//     // const data = useContext(Context);

//     console.log(`hellooooo,${[lat,lon]}`);
// }
